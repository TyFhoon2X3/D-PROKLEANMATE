import ResetPasswordForm from '@/components/ResetPasswordForm';

export const metadata = { title: 'ตั้งรหัสผ่านใหม่ | D-ProKleanMate' };

export default function ResetPasswordPage() {
  return <ResetPasswordForm />;
}